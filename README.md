# 🤖 Text-to-SQL Аналитический Бот

## 📖 Описание

Проект представляет собой модульную систему для выполнения аналитических запросов к базе данных PostgreSQL на естественном языке через Telegram-бот. 

1.  **Telegram Bot:** Интерфейс на **Aiogram 3**.
2.  **FastAPI Backend:** Принимает запрос от бота.
3.  **LLM Service:** Использует **Gemini 2.5 Flash** для генерации SQL-запроса.
4.  **PostgreSQL:** Хранит и выполняет запросы к данным.

## 🛠️ Стек Технологий

* **Backend:** Python 3.11+ (FastAPI)
* **LLM:** Gemini 2.5 Flash
* **База данных:** PostgreSQL (asyncpg)
* **Бот:** Aiogram 3.x
* **Деплой:** Docker Compose

## 🚀 Настройка и Запуск

> **⚠️ КРИТИЧЕСКОЕ ПРИМЕЧАНИЕ:** Из-за локальных проблем с включением виртуализации (WSL/Hyper-V) запуск через Docker Compose не был полностью протестирован.

### 1. Переменные Окружения (`.env`)

Создайте файл `.env` со следующими ключами:

* `GEMINI_API_KEY`: Ваш ключ для Google Gemini.
* `TELEGRAM_BOT_TOKEN`: Токен вашего бота.
* `DATABASE_URL`: Строка подключения к PostgreSQL (например, `postgresql://user:password@db:5432/analytics_db`).

### 2. Запуск вручную (Основной метод)

Убедитесь, что файлы `schema.sql` и `videos.json` находятся в корне проекта.

1.  **Установка зависимостей:** `pip install -r requirements.txt`
2.  **Запуск БД:** Запустите PostgreSQL (локально или через отдельный Docker-контейнер).
3.  **Загрузка данных:** `python data_loader.py`
4.  **Запуск API:** `uvicorn main:app --reload --host 0.0.0.0 --port 8000`
5.  **Запуск Бота:** `python bot.py`

## 💬 Использование

Начните диалог с ботом в Telegram и задайте вопрос, используя термины из вашей схемы БД:

* **Пример:** "Сколько всего видео?"
* **Пример:** "Какой средний прирост просмотров за последнюю неделю?"

## 📦 Структура Проекта

* `main.py`: FastAPI Backend (API-маршруты, управление пулом БД)
* `llm_service.py`: Логика генерации SQL через Gemini
* `bot_service.py`: Логика Telegram-бота
* `data_loader.py`: Скрипт для ETL (загрузка данных в БД)
* `logging_config.py`: Централизованная настройка логгирования
* `requirements.txt`: Зависимости
